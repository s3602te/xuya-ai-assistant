# 多智能體 AI 面試助理 — 踩坑筆記

> 這份筆記記錄的是「航空母艦戰鬥群」（LangGraph 多智能體系統）開發過程中，
> 每一個真實踩到的坑：怎麼發現的、根因是什麼、怎麼修的，
> 以及最重要的 —— **從中歸納出的通用原則**。
>
> 面試時的用法：不要照念症狀，講「通用原則」那一欄。
> 面試官想聽的不是「我遇到過 bug」，而是「我從 bug 裡學到了什麼可遷移的判斷力」。

---

## 目錄

| # | 坑 | 一句話原則 |
| --- | --- | --- |
| 1 | 知道答案卻一直重查 | 狀態要顯性化，不要藏在文字裡 |
| 2 | 掃關鍵字判斷成敗 | 用旗標判斷狀態，不要猜字串 |
| 3 | 檢索門檻從未觸發 | 參數要量出來，不要憑感覺訂 |
| 4 | 調參數不如補問法 | 先確認瓶頸在資料還是在參數 |
| 5 | 提示詞肥大壓垮小模型 | 提示詞是預算，不是許願池 |
| 6 | 白名單永遠追不完 | 列舉有限的那一邊 |
| 7 | 小模型濫用組合函式 | 提示是請求，程式碼才是保證 |
| 8 | 「答對」與「壞掉」不能共用同一個值 | 空值不能同時代表兩件事 |
| 9 | 歷史對話污染 | 給模型「無從抄襲」的素材 |
| 10 | 給太多相似素材造成複讀 | 冗餘不等於豐富 |
| 11 | 一題一題教公式 | 累積不變條件，不要累積公式 |
| 12 | 列舉垃圾字元 | 定義合法，不要列舉非法 |
| 13 | 搜尋摘要撈數字不可靠 | 精確性領域要用專用工具 |
| 14 | 設定改了卻沒生效 | 提防靜默失敗 |
| 15 | 個資進了知識庫 | 可被任意查詢的介面不放個資 |
| 16 | 同一題兩次跑出不同記法 | 修好一次不等於修好了模型 |
| 17 | 回歸測試腳本第一次真正上線就抓到新 bug | 檢查清單抓到的錯誤，才是它存在的意義 |
| 18 | 循序與平行的邊界 | 有依賴與上限走循序，可累加與獨立走平行 |

---

## 1. 知道答案卻一直重查

**症狀**
問「台北 101 和東京晴空塔差多少公尺」，系統對同一個關鍵字連續搜尋三次，
最後還是答不出來，Brave API 額度白白燒掉。

**診斷**
看 log 發現主管節點的 reasoning 裡明明寫著「已查到 508 公尺」，
但下一步又派人去查同一件事。

**根因**
主管每一步都從零重新判斷。它的推理過程只被 `print()` 出來給人看，
**沒有存進共用狀態**，所以下一輪它完全不記得上一輪做過什麼。
資訊存在於「模型當下的輸出文字」裡，而不是存在於「系統的狀態」裡。

**解法**
加一個規劃節點，開場一次性把問題拆成任務清單（每項有 `status`），
主管改成純 Python 逐項勾選 —— 不呼叫模型，因此不會產生幻覺也不會迴圈。
另外建立「事實帳本」，查到的數值登錄進去，後續節點只讀帳本。

**通用原則**
> **狀態要顯性化。** 如果一個資訊「只存在於模型的輸出文字裡」，
> 那它等於不存在 —— 下一次呼叫時模型不會記得。
> 凡是要跨步驟使用的東西，都必須寫進結構化的狀態欄位。

---

## 2. 掃關鍵字判斷成敗

**症狀**
搜尋明明成功，品管節點卻判定失敗並要求重試；
反過來，計算節點回了一個孤零零的 `508`（根本沒做任何運算），卻被判定合格。

**根因**
品管節點是用「掃描回覆文字裡有沒有『錯誤』兩個字」來判斷成敗的。
網頁摘要裡出現「404 錯誤排除教學」就被誤殺；
而 `508` 語法合法，掃不到負面字眼就過關。

**解法**
每個工具節點回報時，一律在訊息最前面掛明確旗標
（`【STATUS:OK】` / `【STATUS:FAIL】`），品管只看旗標，不讀內文。

**通用原則**
> **狀態判斷要靠旗標，不要靠猜字串。**
> 自然語言的表面特徵（有沒有某個詞）和真實語意（成功還失敗）沒有可靠對應。
> 這個錯誤我後來又犯了一次（見第 11 條），代價是一個算對的答案被丟掉。

---

## 3. 檢索門檻從未觸發

**症狀**
RAG 雙軌架構中，「高精準人工問答庫」設了距離門檻 2.0，
但系統上線以來**一次都沒有命中過**，所有問題都掉到精度較低的自動擴展軌。

**診斷**
寫了一支校準腳本，準備兩組對照題目：
一組是「答案確實在知識庫裡」的問題（刻意用跟原文不同的問法），
一組是「應該走搜尋工具」的問題，實測兩組的分數分佈。

結果：應該命中的那組，分數落在 1.8 ～ 6.2；不該命中的落在 4.4 ～ 10.1。
門檻 2.0 比「應該命中組」的最小值還小 —— 難怪永遠不觸發。

**根因**
所用的 embedding 模型輸出的向量**沒有正規化**，
而向量資料庫預設用 L2 距離 —— 距離被向量長度主導，語意相似度反而被稀釋。
門檻 2.0 是照「餘弦相似度」的直覺訂的，但實際跑的是 L2，兩者尺度完全不同。

**解法**
改用餘弦距離重建 collection。同一組測試下分離度從 −0.615（重疊）
變成 +0.198（完全分得開），門檻由校準腳本直接算出建議值。

**通用原則**
> **參數要量出來，不要憑感覺訂。**
> 尤其是「相似度門檻」這種抽象數字，人的直覺完全不可靠。
> 建立一組正反對照題目，把分佈畫出來，門檻自然浮現。
> 而且這支腳本可以一直留著 —— 每次擴充知識庫後重跑，確認沒有退化。

---

## 4. 調參數不如補問法

**症狀**
知識庫調校時，一直想靠調門檻、換演算法來提升命中率。

**診斷**
校準腳本印出了一個關鍵數字：
同一個主題，問法幾乎照抄知識庫原文時相似度 0.905，
改寫成自然問法後只剩 0.573 ～ 0.853。

**根因**
使用者不會照著知識庫的原文打字。真正的瓶頸不在檢索演算法，
而在**知識庫只寫了一種問法**。

**解法**
每個主題撰寫三到五種不同問法（換詞、換語氣、換切入角度），
共用同一份答案。知識庫從 10 筆擴充到 105 筆，其中大部分是問法變體而非新內容。

**通用原則**
> **先確認瓶頸在資料還是在參數。**
> 調參數的邊際效益通常遠低於補資料，但因為調參數比較「像在做技術」，
> 很容易不知不覺花掉大量時間在錯的地方。

---

## 5. 提示詞肥大壓垮小模型

**症狀**
每修一個問題就往提示詞加一條規則，累積到 11 條鐵則 + 5 個資料區塊後，
模型開始出現詭異行為：
- 把提示詞的區塊標題原封不動印給使用者：「（本輪任務完成情況：所有項目都已完成）」
- 明明檢索到了正確資料卻視而不見，直接把上一題的答案抄一遍

**根因**
本地模型的上下文上限是 4096 token，中文一個字約一個 token。
提示詞塞滿之後，模型分不清「哪些是要照做的指令」「哪些是要引用的資料」
「哪些是骨架不該講出來」—— 全部糊在一起。
更糟的是超出的部分會被**靜默截斷**，最先被吃掉的通常是最前面的身分設定。

**解法**
依情境分兩套提示詞：
- 知識庫命中且未動用工具（約 90% 的情況）→ 極簡提示：人設 + 問題 + 參考答案 + 4 條短規則
- 真的用到搜尋／計算工具 → 才用完整版

另外把佐證用的內容主動壓短，把上下文預算留給真正要引用的資料。

**通用原則**
> **提示詞是預算，不是許願池。**
> 每加一條規則都在稀釋其他規則的權重。
> 與其寫一份包山包海的提示詞，不如依情境給不同複雜度的版本 ——
> 簡單的任務就給簡單的提示。

---

## 6. 白名單永遠追不完

**症狀**
要判斷「這題是不是純數學題（不需上網）」，
一開始的做法是列舉數學動詞：加、減、乘、除、相差、平分……

結果每測一題新的就要補一次清單：
- 「10 片披薩分 4 個人」→ 漏了「片」
- 「10 顆糖果分 6 個人」→ 漏了「顆」
- 「大魚 70 條分 7 個人」→ 漏了「條」

漏掉的那次後果很嚴重：系統真的拿「大魚總數」去搜尋引擎，撈回「一午二紅沙」這句俗諺。

**根因**
中文的數學講法是**無限集合**，白名單這條路本質上走不通。

**解法**
把問題反過來問：不要問「這是不是數學題」，要問「**這題需不需要外部事實**」。

```
題目裡有兩個以上的數字  +  沒有任何外部事實訊號  →  自足的數學題
```

外部事實的訊號是**有限且穩定**的：上網、查詢、最新、現任、股價、匯率、天氣、誰是……
這份清單不會膨脹，因為外部事實的種類本來就有限。

改完之後 9 個測試案例全部正確，而且完全不需要認識「片」「顆」「條」這些量詞。

**通用原則**
> **列舉有限的那一邊。**
> 當你發現自己在維護一份「永遠會漏」的清單時，
> 停下來問：有沒有辦法把條件反過來寫，讓要列舉的變成有限集合？

---

## 7. 小模型濫用組合函式

**症狀**
所有的計算錯誤都長得很像：

```
「糖果25顆分給7個人」  → comb(25, 7) - (7 * 7)   = 480651
「10 除以 6」          → comb(6, 1) / comb(10, 1) = 0.6
「大魚70條分7人」      → comb(140, 1) / comb(1, 1) = 140
「蘋果60檸檬40分9人」  → comb(10,2)*comb(5,1)/comb(3,1) - comb(7,1) - ...（長到把 log 洗爆）
```

**根因**
負責翻譯算式的小模型學到了一個錯誤的關聯：「數學任務 → 用 comb」，
不管題目是不是組合問題。
而這些算式**通過了所有既有檢查** —— 語法合法、數字確實來自題目 —— 所以攔不住。

**解法**
兩層一起做：
1. 提示層：明確寫「comb/perm/factorial 只能用在題目問組合、排列、選法的時候」
2. **程式層**：實際檢查題目文字有沒有「組合／排列／幾種／選法」，沒有就直接擋下

實測 8 個案例全部正確分流。

**通用原則**
> **提示詞是請求，程式碼才是保證。**
> 只要一件事「絕對不能發生」，就不能只寫在提示詞裡 ——
> 小模型不會照做，大模型也只是機率上比較配合。
> 能用程式碼驗證的，就用程式碼驗證。

---

## 8. 「答對」與「壞掉」不能共用同一個值

**症狀**
問「請自我介紹一下」，log 顯示規劃節點正確判斷「這題不需要任何工具」，
但下一行主管卻把它派去搜尋引擎，撈回一堆「30秒自我介紹範本」。

**根因**
規劃節點的兩種結論被壓縮成同一個值：
- 「我判斷這題不需要工具」→ 回傳空清單
- 「我拆解失敗了」→ 也是空清單

主管收到空清單時無從分辨，只好一律走「LLM 自由判斷」的保底路徑。
**規劃節點答對了，但答案在傳遞過程中遺失了。**

**解法**
把管道加寬成三態：`has_plan` / `no_tools_needed` / `undetermined`。
主管看到 `no_tools_needed` 就直接結案，零 API 呼叫。

**通用原則**
> **一個值不能同時代表兩件事。**
> 「空」「null」「0」這類值特別容易被拿來表達多種含義，
> 而下游只要分不清楚，上游再正確的判斷都會白費。
> 這個坑我在同一個專案的不同層級遇到了兩次。

---

## 9. 歷史對話污染

**症狀**
連續問幾題之後，每一題的回答開頭都變成完整的自我介紹，
而且越積越長。更嚴重的是有一題查詢與計算都成功了，
最終回覆卻說「資訊不足」。

**根因**
第一題的自我介紹被存進對話歷史，之後每題都被當成「參考歷史」塞進提示詞。
小模型分不清「參考」和「照抄」，就把整段自我介紹一遍遍複製到開頭。
而那段被反覆抄寫的文字把有限的上下文塞爆，
**真正的搜尋結果與計算數字被擠出去了** —— 所以它說資訊不足。

**解法**
三層一起做：
1. 歷史只保留最近一輪，不是全部
2. 每則截短到 60 字
3. 最關鍵的：知識庫命中時，**只給模型「上一題的問題」，不給「上一題的答案」**
   —— 模型仍能理解「那個」「裡面」指的是什麼，但手上沒有可以抄的答案文本

**通用原則**
> **與其叫模型「不要抄」，不如讓它「無從抄起」。**
> 限制模型能看到的素材，比在提示詞裡增加禁令有效得多。

---

## 10. 給太多相似素材造成複讀

**症狀**
問「談談你的 DevOps 與 CI/CD 經驗」，回覆把同一句話連續輸出了幾十遍，
整個聊天視窗被洗版。

**根因**
這是**修上一個問題時造成的迴歸**。
為了避免模型因為素材不足而編造，我讓檢索器一次帶回同主題的前 3 筆。
但 DevOps 那個主題底下三題的答案幾乎一模一樣，
模型讀到三段近乎複製貼上的文字，就卡進了複讀迴圈。

**解法**
1. 檢索器帶回多筆素材時，先比對答案開頭做去重
2. 最終輸出再加一道複讀偵測，重複句一出現就截斷

**通用原則**
> **冗餘不等於豐富。**
> 「多給一點素材」這種直覺上安全的做法也有副作用。
> 每一個修正都要問一次：這個改動會不會在別的地方製造新問題？

---

## 11. 一題一題教公式

**症狀**
數學應用題一直算錯，而且每次錯的方式都不一樣。
很自然的念頭是：把分配題、比例題、追及問題的公式一條條寫進提示詞。

**根因（更深一層）**
這跟第 6 條是**同一個陷阱換了個位置** —— 中文應用題的型態是無限集合，
公式清單永遠寫不完。

**解法**
不要累積公式，要累積**不變條件** —— 任何正確答案都必須滿足的性質。
這類條件數量很少而且穩定，目前累積了四條：

| # | 不變條件 | 擋掉什麼 |
| --- | --- | --- |
| 1 | 算式的數字必須有來源（題目或已查證的事實） | 憑空捏造 |
| 2 | 算式必須真的含有運算 | 孤立數字 `508` |
| 3 | 組合函式只用於組合題 | `comb(25,7)` 濫用 |
| 4 | 題目提到的數量都必須被用到 | 算漏了 |

第 4 條抓到的實例：
題目「有 60 顆蘋果、40 顆檸檬，分給 9 個人」，
算式只有 `60 // 9 ; 60 % 9` —— **檸檬的 40 從頭到尾沒被碰過**。
單看算式完全合法，但少了這條檢查就會給出「每人各拿 6 個蘋果和檸檬」這種錯誤答案。

（實作細節：判斷「數字是數量還是專有名詞的一部分」時，
看數字後面有沒有量詞 —— 70**條**、60**顆**是數量，台北**101**高度不是。）

**通用原則**
> **累積不變條件，不要累積公式。**
> 公式是「怎麼做對」，數量無窮；
> 不變條件是「什麼情況一定是錯的」，數量有限。
> 而且不變條件通常能用純程式碼驗證，不依賴模型能力。

---

## 12. 列舉垃圾字元

**症狀**
同一道數學題，換個問法就一次成功、一次完全失敗。

**診斷**
比對兩次的算式抽取結果：

```
失敗：60 // 9 ; 60 % 9 ; 40 // 9 ; 40 % 9}     ← 尾巴多一個 }
成功：60 // 9 ; 60 % 9 ; 40 // 9 ; 40 % 9
```

**模型兩次都答對了**，差別只有一個字元。

**根因**
更早之前模型吐出過 `:(60 // 9) + (40 // 9)`（開頭一個冒號），
當時的修法是「列舉要清掉的垃圾字元」：冒號、等號、引號……
但 `}` 不在清單裡，於是同一類問題又發生一次。

**這是第 6 條的原則，我自己沒有貫徹。**

**解法**
改成定義合法邊界：一條算式的開頭只可能是數字、左括號、或函式名的第一個字母；
結尾只可能是數字或右括號。其餘從邊緣剝掉。
（細節：判斷函式名開頭時要限定 ASCII 字母，
否則「答案：634 - 508」的「答」會因為 `isalpha()` 為真而被留下來。）

改完之後 7 種雜訊格式全部正確處理。

**通用原則**
> **定義合法，不要列舉非法。**
> 跟第 6 條是同一件事。值得記住的是：
> **知道原則和貫徹原則是兩回事** —— 我在同一個專案裡把同一個原則忘了兩次。

---

## 13. 搜尋摘要撈數字不可靠

**症狀**
- 問台積電股價，回答 2355，實際收盤 2375
- 問喜馬拉雅山最高峰，回答 90000 公尺（實際 8848）
- 問台灣最高峰，回答 3193 公尺（實際 3952）

**診斷**
去查那個 3193 的來源，發現它出自維基百科同一篇文章裡
「日本北岳」的高度 —— 萃取器抓錯行了。
股價的 2355 則多半來自「跌破 2355 支撐」這類分析文章。

**根因**
從搜尋引擎的**摘要片段**撈數字，本質上就不可靠。
摘要是為了給人看的上下文，不是結構化資料。

**解法（暫時）**
在回覆時明確標註「這是網路搜尋結果，請以官方即時資料為準」。

**解法（正解，尚未實作）**
替精確性要求高的領域接**專用 API**，而不是爬搜尋摘要。
這跟「不准模型心算，一律交給計算機」是同一個模式。

**通用原則**
> **精確性要求高的領域，要給它專用工具。**
> 判斷方式：如果答錯的代價很高（金額、劑量、法規），
> 就不該從非結構化文字裡撈答案。

---

## 14. 設定改了卻沒生效

**症狀**
把向量資料庫的距離度量從 L2 改成餘弦，程式沒報錯，
但實際查詢的分數尺度完全沒變。

**根因**
向量資料庫的距離度量是**建立 collection 當下就固定**的。
對已存在的 collection 傳入新的設定，它會**安靜地忽略**，不報錯也不警告。

**解法**
1. 改設定時要刪除並重建 collection，不能只改參數
2. 更重要的：**在服務啟動時把實際生效的設定印出來**

```
[系統] ✅ ChromaDB 連接完成：手動精準軌=105筆/cosine | 自動擴展軌=2筆/cosine
[系統] ⚠️ 兩軌的距離度量不一致（手動=cosine / 自動=l2）
```

**通用原則**
> **提防靜默失敗。** 「沒報錯」不等於「有生效」。
> 凡是有「設定可能沒吃到」風險的地方，都要主動把實際狀態印出來驗證。
> 這種 bug 最難查，因為所有表面跡象都是正常的。

---

## 15. 個資進了知識庫

**症狀**
（這一條是事前避免，不是事後補救）
準備把履歷 PDF 的內容匯入知識庫時，注意到裡面有手機號碼、Email、完整住址。

**根因**
這個 AI 是**任何人都能對它提問**的介面。
把個資放進知識庫，等於把它放在一個可被任意查詢的公開端點上。

**解法**
明確劃出不進知識庫的清單：
- 手機、Email、完整住址等個資
- 針對特定公司客製的自傳段落（換一家面試就尷尬）
- 舊的薪資紀錄

**通用原則**
> **資料進入系統前，先問「誰能查到它」。**
> 尤其是 RAG 這種「把資料變成可對話介面」的架構，
> 匯入的門檻比傳統資料庫更需要謹慎 —— 因為查詢方式是開放的自然語言。

---

## 16. 同一題兩次跑出不同記法

**症狀**
同一種題型（「A 部門選 a 人、B 部門選 b 人，共有幾種選法」）測了兩次，
文字幾乎一模一樣，結果卻是兩種截然不同的行為：

```
第一次：算式翻譯機吐出 C(8,3) * C(6,2)     → 白名單只認 comb/perm/factorial，
                                             "C" 不合法，重試三次後放棄，轉真人
第二次：算式翻譯機吐出 comb(8,3) * comb(6,2) → 直接算對，答案 840
```

**根因**
提示詞完全沒有改變，模型也沒有變，兩次卻選了不同的函式記法。
這是小模型的**取樣變異**：同一份提示詞、同一個問題，
不保證每一次都輸出同一種寫法。第一次踩到的坑，
本質上一直都在，只是這一次剛好沒有被觸發。

**解法**
把「模型很可能會用的同義記法」在送進白名單檢查之前先正規化掉，
而不是繼續在提示詞裡加字勸它「請用 comb」：

```python
expr = re.sub(r'\bC\s*\(', 'comb(', expr)
expr = re.sub(r'\bP\s*\(', 'perm(', expr)
```

這樣不管模型這次心情好不好、選哪種記法，白名單檢查看到的都是同一種形式。

**通用原則**
> **修好一次不等於修好了模型。**
> 小模型的輸出有取樣變異，同一個 bug 用同一份提示詞測，
> 這次沒發生不代表下次不會發生。真正該問的問題不是「這次過了嗎」，
> 而是「如果它下次又選了別的寫法，程式碼有沒有兜住」。
> 這也是為什麼第 7 條、第 12 條的解法都刻意選在**程式層**而不是提示層 ——
> 提示層的修正效果無法穩定重現，程式層的正規化才是。
> 實務作法：把每一個修過的 bug 都收進一份回歸測試清單，
> 每次改提示詞或換模型版本，就把整份清單重新跑一次 ——
> 「這次沒報錯」跟「這個 bug 真的不會再發生」是兩件事，
> 沒有回歸測試就無法區分「修好了」跟「這次運氣好」。

---

## 17. 回歸測試腳本第一次真正上線就抓到新 bug

**症狀**
把踩坑筆記裡的舊案例整理成回歸測試腳本後，第一次完整跑一遍，
「大魚小魚分配題」這個早就標記為「已修好」的案例卻不通過：

```
[算盤法師 Math_Agent] 正在使用魔法計算機: ((70 // 7) + (30 // 7))
[算盤法師 Math_Agent] 正在使用魔法計算機: (70 % 7)
[算盤法師 Math_Agent] 正在使用魔法計算機: (30 % 7)
...
實際回覆：每個人可以分到 2 條大魚、14 條小魚，各會剩下 0 條大魚和 2 條小魚。
```

正確答案應該是「10 條大魚、4 條小魚」，模型卻把 `70 // 7` 和 `30 // 7`
兩個分屬不同東西的商【加在一起】變成 14，之後盜賊客服在寫回覆時，
只好從一個語意錯誤的帳本裡瞎猜哪個數字對應大魚、哪個對應小魚，兩個都猜錯位置。

**根因**
提示詞第 6 條規則其實已經用「蘋果／檸檬」示範過「兩種東西要分開算、不能合併」，
但换成「大魚／小魚」、換一種「幾條 A、幾條 B」的句型，小模型沒有把規則類推過去。
這是本專案一路以來反覆驗證的事實：**教規則不保證每次都被遵守**，
尤其換一種措辭、換一種句型，覆蓋率就會有落差。

**解法**
既有的四道不變條件（語法合法、必須有運算、數字有來源、題目數字都用到）
全部檢查不出這個錯誤——因為 70、30、7 三個數字都確實用到了，
算式也真的有在運算，問題不是「數字」，是「兩個該分開的量被合併」。

補上第五道不變條件，直接用 AST 檢查算式的語法樹結構：
如果一條算式的**最外層**是「兩個取整除或取餘數的結果相加」，
不管題目講什麼，這幾乎不可能是正確答案——分配題要的是「各自」的商，
不是把商數加總。這個檢查不看文字、不看關鍵字，直接看語法樹的形狀，
所以換任何措辭、任何量詞都擋得住，不會像提示詞範例那樣受句型影響。

```python
def _is_merged_quotients(e: str) -> bool:
    node = ast.parse(e, mode="eval").body
    def _is_quotient_or_remainder(n):
        return isinstance(n, ast.BinOp) and isinstance(n.op, (ast.FloorDiv, ast.Mod))
    return (isinstance(node, ast.BinOp) and isinstance(node.op, ast.Add)
            and _is_quotient_or_remainder(node.left)
            and _is_quotient_or_remainder(node.right))
```

**通用原則**
> **檢查清單抓到的錯誤，才是它存在的意義。**
> 這支回歸測試腳本第一次真正派上用場，不是全部綠燈讓人安心，
> 而是它抓到了一個連開發者自己都還沒發現的錯誤。
> 如果一份回歸測試清單長期保持「全部通過」，該問的不是
> 「太好了都沒問題」，而是「這份清單夠不夠嚴格，還是只是在自我安慰」。
> 這也再一次印證第 11、16 條的教訓：能寫成 AST／程式碼驗證的不變條件，
> 永遠比多寫一個提示詞範例更可靠——範例只覆蓋你想到的措辭，
> 語法樹結構的檢查不管換什麼措辭都一樣管用。

---

## 附錄：整個過程中最重要的三個轉折

### 轉折一：從「調模型」轉向「調系統」

早期的直覺是「答錯了 → 提示詞再寫清楚一點」。
但實測發現，本地 8B 模型在同樣的提示詞下表現並不穩定，
而且每加一條規則就稀釋其他規則。

真正有效的是把判斷從模型手上收回程式碼 ——
規劃節點、事實帳本、狀態旗標、不變條件檢查，
這些都不依賴模型能力，因此結果可預測。

### 轉折二：從「列舉」轉向「反轉」

白名單追不完（第 6 條）、垃圾字元列舉不完（第 12 條），
兩次都是同一個解法：把條件反過來寫，讓要列舉的變成有限集合。

### 轉折三：從「怎麼做對」轉向「什麼一定是錯的」

教模型公式是無窮盡的（第 11 條）；
但「正確答案必須滿足什麼性質」是有限的，而且能用程式碼驗證。

---

## 18. 循序與平行的邊界：共享狀態與依賴競爭

**症狀**
在設計多任務與多 Agent 系統時，為了追求極致速度想讓所有步驟同時並行，
（如邊搜尋邊計算、多個 Agent 同時扣減額度），結果出現「計算步驟拿到空值/猜測值」，
或是「多方同時修改同一狀態導致帳目覆蓋錯亂」。

**診斷**
硬體（如 GPU）的並行是無相依的資料矩陣運算，但業務邏輯具有「時序因果」與「共享邊界」。
若把具有相依性（A 完成才能做 B）或具有邊界上限（共享預算/額度扣除）的任務盲目平行化，
系統會產生競態條件（Race Condition）與幻覺骨牌效應。

**根因**
混淆了「可獨立運算」與「具依賴性」的任務邊界：
1. **依賴鏈未斷開**：下游需要上游的產出作為輸入（如：查到建築高度才能算差值），未給資料就搶跑。
2. **狀態寫入衝突**：多個並行單元直接回報「最終餘額」（覆蓋寫入），而非回報「事件明細」（追加寫入）。

**解法**
1. **依賴強制排序**：在任務規劃階段，強制將無依賴的檢索（Search）置於計算（Math）之前，排定 Pipeline 順序。
2. **狀態機單步派工與檢查點**：主管（Supervisor）採循序勾選，每個 Agent 產出後需通過 Grader 驗證才能推進到下一動。
3. **從「覆蓋餘額」改為「收據模式（Reducer）」**：若必須平行執行，各節點僅准許提交操作明細（如：`[-50, -30]`），最後由單一結算節點統一匯總，嚴禁各節點自行計算並覆蓋全域狀態。
4. **關鍵資源設硬鎖**：若任務涉及絕對硬上限（如庫存剩 1、預算不可超支），強制關閉平行，改走單一隊列循序扣除。

**通用原則**
> **有依賴與上限的走循序，可累加與無關聯的走平行。**
> 平行跑省的是時間，循序跑保的是正確性。
> 凡是涉及到「下游吃上游資料」或「爭奪共享上限資源」的操作，程式碼層級必須設立排隊與檢查點，不可盲目搶快。

---

## 附錄：這些坑的代價與收穫

| 面向 | 改造前 | 改造後 |
| --- | --- | --- |
| 重複查詢 | 同一關鍵字連查 3 次 | 硬去重，一次不多查 |
| 履歷題 API 消耗 | 每題都上網 | 零 API 呼叫 |
| 高精準知識庫 | 從未觸發 | 實測全部命中正確主題 |
| 檢索分離度 | −0.615（重疊） | +0.198（分得開） |
| 門檻決定方式 | 憑感覺 | 校準腳本量測 |
| 數學正確性 | 憑模型心算 | 四道不變條件把關 |
| 組合數記法變異 | 沒有回歸測試，靠單次測試判斷「修好了」 | 記法正規化 + 累積回歸測試清單 |
| 兩種東西的商被合併 | 提示詞範例覆蓋不到每一種措辭 | AST 檢查算式語法樹結構，不受措辭影響 |
| 任務調度與狀態依賴 | 盲目並行導致依賴搶跑、餘額覆蓋錯亂 | 依賴強制排序 + 狀態機單步派工，兼顧效能與正確性 |

---

*本筆記持續更新中。每次踩到新坑就補一條，重點永遠放在「通用原則」那一欄。*